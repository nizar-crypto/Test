var readline = require("readline-sync"); // here I watched a video javascript and parameters

const name = " Nizar Ker";
function upperCase() {
  //() parameter is what we inject a value or a variable to be executed in the function

  console.log("part time");
  console.log("part time");
  console.log("part time");
  console.log("part time");
}

function adder(num1, num2) {
  console.log(num1 + num2);
}
adder(4 + 9); //here we inject the value in the parameter to pass down in the function and add the number together

function toUpperCase() {
  //text. adding .(dot) means we can add another function inside the function
  //   text.toUpperCase();
  //   console.log(text); or we can use another thing
}
